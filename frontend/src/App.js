import React, { Component } from 'react';
import Loader from './TimeSeriesLoader';

export default class App extends Component {

  render() {

      return (
          <div>
              <Loader />
          </div>
      );
  }
}
